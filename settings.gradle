pluginManagement {
    repositories {
        google()
        mavenCentral()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        mavenCentral()
    }
}

rootProject.name = "OTTPlayerLite"
include ':app'